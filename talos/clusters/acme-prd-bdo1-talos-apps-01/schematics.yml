# Cluster Image Factory schematic layer (Talos v1.15.0-alpha.0): additive
# env-wide entries merged over _base; node layers add node-specific entries.
# Bare `siderolabs/<name>` entries only — the factory pins versions to the release.
customization:
  systemExtensions:
    officialExtensions:
      - siderolabs/netbird # overlay mgmt plane for all nodes in this cluster
