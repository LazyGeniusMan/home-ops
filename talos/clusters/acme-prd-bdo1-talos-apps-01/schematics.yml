# Cluster Image Factory schematic layer (Talos v1.14): additive env-wide
# entries merged over the _base layer; node layers add node-specific entries.
# Bare `siderolabs/<name>` entries only — the factory pins extension versions
# to the Talos release. This layer's entries apply to all nodes in
# acme-prd-bdo1-talos-apps-01.
# Upload: curl -X POST --data-binary @schematics.yml https://factory.talos.dev/schematics
customization:
  systemExtensions:
    officialExtensions:
      - siderolabs/netbird # overlay mgmt plane (contrib; verified in extensions release-1.14 README)
