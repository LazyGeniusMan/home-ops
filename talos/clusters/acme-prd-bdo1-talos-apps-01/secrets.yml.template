# Template for acme-prd-bdo1-talos-apps-01 secrets (Talos v1.14).
# secrets.yml itself is GITIGNORED (see talos/.gitignore) — generate it
# (never commit, never inject):
#   talosctl gen secrets -o secrets.yml   # cluster PKI bundle
# Registry credentials live in patches.yml (RegistryAuthConfig docs rendered
# via pass-cli inject into the gitignored build/), not here.
