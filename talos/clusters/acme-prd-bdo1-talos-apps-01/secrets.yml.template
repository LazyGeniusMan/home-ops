# Manual input for acme-prd-bdo1-talos-apps-01 secrets (Talos v1.15.0-alpha.0).
# Not read by automation (day-0 mints ansible/build/<cluster>/secrets.bundle.yml).
# secrets.yml is GITIGNORED — generate, never commit:
#   talosctl gen secrets -o secrets.yml
