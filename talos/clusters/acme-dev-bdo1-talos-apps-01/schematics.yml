# Cluster schematic layer (Talos v1.15.0-alpha.0): env-wide entries over _base.
# Bare `siderolabs/<name>` entries only (factory pins versions to the release).
customization:
  systemExtensions:
    officialExtensions:
      - siderolabs/netbird # mgmt plane for all nodes in this cluster
