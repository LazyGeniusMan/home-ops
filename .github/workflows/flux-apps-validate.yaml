# Root wiring for apps validation. This file carries the triggers and calls
# the canonical job logic in flux/apps/.github/actions/validate-manifests via
# the area-local reusable workflow flux/apps/.github/workflows/validate.yaml
# (workflow_call).
name: flux-apps-validate

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'flux/apps/**'
      - 'flux/fleet/tenants/apps.yaml'
  push:
    branches: [ 'main' ]
    paths:
      - 'flux/apps/**'
      - 'flux/fleet/tenants/apps.yaml'

permissions:
  contents: read

jobs:
  manifests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - name: Validate manifests
        uses: ./flux/apps/.github/actions/validate-manifests
        with:
          script: ./flux/scripts/validate.sh
          validate-dir: flux/apps
