schema-version = "1.15.0"

[install]
ansible.pkg-path = "ansible"
ansible.version = "2.21.4"
cilium-cli.pkg-path = "cilium-cli"
cilium-cli.version = "0.19.7"
fluxcd.pkg-path = "fluxcd"
fluxcd.version = "2.9.5"
fluxcd-operator-mcp.pkg-path = "fluxcd-operator-mcp"
fluxcd-operator-mcp.version = "0.58.1"
kubectl.pkg-path = "kubectl"
kubectl.version = "1.37.0"
github-cli.pkg-path = "github-cli"
github-cli.version = "2.101.0"
go.pkg-path = "go"
go.version = "1.26.7"
gopls.pkg-path = "gopls"
gopls.version = "0.23.0"
kubernetes-helm.pkg-path = "kubernetes-helm"
kubernetes-helm.version = "4.3.0"
kustomize.pkg-path = "kustomize"
kustomize.version = "5.8.1"
netbird.pkg-path = "netbird"
netbird.version = "0.79.0"
proton-pass-cli.pkg-path = "proton-pass-cli"
proton-pass-cli.version = "2.3.3"
python3.pkg-path = "python3"
python3.version = "3.14.7"
opentofu.pkg-path = "opentofu"
opentofu.version = "1.12.6"
golangci-lint.pkg-path = "golangci-lint"
golangci-lint.version = "2.13.2"
govulncheck.pkg-path = "govulncheck"
govulncheck.version = "1.8.0"
ansible-lint.pkg-path = "ansible-lint"
ansible-lint.version = "25.8.2"
yq.pkg-path = "yq-go"
yq.version = "4.53.3"
kubeconform.pkg-path = "kubeconform"
kubeconform.version = "0.8.0"
# CI validation/test tools (pinned to latest catalog versions 2026-09-24).
cosign.pkg-path = "cosign"
cosign.version = "3.1.3"
oras.pkg-path = "oras"
oras.version = "1.3.4"
docker.pkg-path = "docker"
docker.version = "29.8.0"
docker-buildx.pkg-path = "docker-buildx"
docker-buildx.version = "0.35.0"
curl.pkg-path = "curl"
curl.version = "8.22.0"
git.pkg-path = "git"
git.version = "2.55.0"
gnutar.pkg-path = "gnutar"
gnutar.version = "1.35"
yamllint.pkg-path = "yamllint"
yamllint.version = "1.37.1"
# EXCEPTION: ControlPlane flux distribution CLI (controlplaneio-fluxcd/distribution/actions/*)
# has no Flox catalog package (verified 2026-09-12: `flox search controlplane` = no match;
# `flox search fluxcd` lists only OSS fluxcd/fluxcd-operator/fluxcd-operator-mcp, which are
# NOT the same binary). CI push legs using the ControlPlane distribution remain the only
# tool invocation without a local Flox-provided equivalent.

[vars]
PROTON_PASS_KEY_PROVIDER = "fs"

[hook]
on-activate = '''
  install_talosctl() {
    cache_dir="${FLOX_ENV_CACHE:-$FLOX_ENV_PROJECT/.flox/cache}"
    mkdir -p "$cache_dir/bin"
    if [ ! -x "$cache_dir/bin/talosctl" ]; then
      curl -L -o "$cache_dir/bin/talosctl" \
        "https://github.com/siderolabs/talos/releases/download/v1.15.0-alpha.0/talosctl-linux-amd64"
      chmod +x "$cache_dir/bin/talosctl"
    fi
  }
  install_talosctl
  cd "$FLOX_ENV_PROJECT" || return
'''

[profile]
common = '''
  export PATH="$FLOX_ENV_CACHE/bin:$PATH"
  alias terraform=tofu
'''

[services]
[services.flux-operator-mcp]
command = '''
  export KUBECONFIG="$FLOX_ENV_PROJECT/kubeconfig"
  flux-operator-mcp serve --transport http --port 8080 --kubeconfig "$KUBECONFIG"
'''

[services.netbird]
command = '''
  sudo mkdir -p /var/run/netbird /var/log/netbird /var/lib/netbird
  sudo env PATH="$PATH" netbird service run --log-file console --log-level info
'''

[options]
allow.unfree = true
