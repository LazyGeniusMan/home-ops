apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cluster.yaml
  - storageclasses.yaml
  - s3-ui-routes.yaml
  - ui-auth.yaml
  - wildcard-certificate.yaml
  - terraform.yaml
  - terraform-remote-state-rbac.yaml
  # SeaweedFS COSI driver (moved from the cosi component — the driver is a
  # SeaweedFS workload, so it lives in this tenant namespace; see README).
  - driver-rbac.yaml
  - driver.yaml
  - bucketclasses.yaml
