# CHI + jobs (S3 prereqs live in controllers/base so the fleet
# infra-controllers -> infra-configs dependsOn chain gates the CHI on bucket
# and Secret readiness). Backup CronJob sorts after the CHI it snapshots.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - installation-base.yaml
  - backup-cronjob.yaml
  - rclone-sync-clickhouse.yaml
  - wildcard-certificate.yaml
