apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
patches:
  # Dev: singletons stay 1 (base values).
  - patch: |
      - op: replace
        path: /spec/values/recommender/replicas
        value: 1
    target:
      kind: HelmRelease
      name: vpa
