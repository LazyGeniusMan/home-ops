# Placeholder configs overlay (Flux Kustomization infra-configs consumes
# ./configs/<environment>). Removed with this directory when the first real
# component lands (§§8-14).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
