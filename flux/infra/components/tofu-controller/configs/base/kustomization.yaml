# Empty on purpose: no `kind: Terraform` objects ship yet (later tasks add
# them here once the controller is live). Env overlays inherit this base.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
