# VPA recommender for the COSI controller Deployment (HPA-paired):
# updateMode Off — recommender-only, because the HPA scales the same
# CPU/memory metrics (known-limitations.md: never VPA-apply on a resource
# HPA scales on). BucketClaims stay VPA-free (Group C).
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: controller
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: controller
  updatePolicy:
    updateMode: 'Off'
