# COSI central controller (release-0.2, v1alpha1 API) + CRDs.
# Vendored from kubernetes-sigs/container-object-storage-interface @
# origin/release-0.2 (f75d4750) — do NOT track main (v1alpha2, incompatible).
# CRDs: group objectstorage.k8s.io, version v1alpha1 (BucketClass,
# BucketClaim, Bucket, BucketAccessClass, BucketAccess), generated via
# controller-gen v0.17.3.
# Namespace: upstream ships `system` with namespace-scoped RBAC bound to
# `default`; this component sets the kustomize `namespace: cosi` (matches the
# Flux tenant namespace) and patches RBAC subjects below, so the controller
# and its leader-election Role live in `cosi`.
# Controller image: staging build of the release-0.2 branch
# (v20250905-controllerv0.2.0-rc1-100-gd904c62). No registry.k8s.io
# promotion exists yet (RELEASE.md keeps the template-project boilerplate;
# cloudbuild publishes only gcr.io/k8s-staging-sig-storage). Tracked via
# flux/infra/update-policies/cosi.yaml ($imagepolicy marker below).
---
kind: Namespace
apiVersion: v1
metadata:
  name: cosi
