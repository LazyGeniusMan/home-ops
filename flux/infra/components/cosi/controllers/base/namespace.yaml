# COSI central controller v0.2.2 (v1alpha1; do not track main/v1alpha2). Vendored from kubernetes-sigs/container-object-storage-interface.
# CRDs in crds/base (fleet prune:false); namespace cosi (upstream system) with RBAC subjects patched. Image tracks update-policies/cosi.yaml (capped <0.3.0).
---
kind: Namespace
apiVersion: v1
metadata:
  name: cosi
