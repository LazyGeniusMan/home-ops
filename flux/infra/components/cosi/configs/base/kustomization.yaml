apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cosi
# COSI central infrastructure only (CRDs + controller live in
# controllers/base). The SeaweedFS COSI driver moved to the seaweedfs
# component's configs/base (it runs in the seaweedfs tenant namespace).
resources: []
