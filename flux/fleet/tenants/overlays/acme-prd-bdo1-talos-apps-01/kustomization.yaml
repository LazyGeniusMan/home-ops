# Per-cluster tenants overlay for acme-prd-bdo1-talos-apps-01 (synced by
# clusters/acme-prd-bdo1-talos-apps-01/tenants.yaml). Pass-through of the
# shared set (apps + infra + policies); no active patches. Patches are
# inline RFC 6902 (see ../README.md).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../apps.yaml
  - ../../infra.yaml
  - ../../policies.yaml
# EXAMPLE (disabled): prd-only allowlist note on top of the cosign verify
# pins. Map-value edit, so no `test` guard — keep the resource index + key
# stable with tenants/policies.yaml.
# patches:
#   - target:
#       group: fluxcd.controlplane.io
#       version: v1
#       kind: ResourceSet
#       name: policies
#     patch: |-
#       - op: add
#         path: /spec/resources/0/data/prd-only-note
#         value: >-
#           prd pins stable tags; see matchOIDCIdentity in tenants/infra.yaml,
#           tenants/apps.yaml, and clusters/*/flux-system/flux-instance.yaml.
