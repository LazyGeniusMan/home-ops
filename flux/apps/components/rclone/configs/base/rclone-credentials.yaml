# Rclone credentials (§12.2): synced by ESO from Proton Pass
# (pass://acme-prd-bdo1-talos-apps-01/rclone/*). Seed the vault entries
# with pass-cli. Drive key names must match the rclone protondrive backend
# options — align them after running `rclone config` once (see README).
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: rclone-credentials
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: proton-pass
    kind: ClusterSecretStore
  target:
    name: rclone-credentials
  data:
    - secretKey: sw-access-key
      remoteRef:
        key: pass://acme-prd-bdo1-talos-apps-01/rclone/sw-access-key
    - secretKey: sw-secret-key
      remoteRef:
        key: pass://acme-prd-bdo1-talos-apps-01/rclone/sw-secret-key
    - secretKey: drive-username
      remoteRef:
        key: pass://acme-prd-bdo1-talos-apps-01/rclone/drive-username
    - secretKey: drive-password
      remoteRef:
        key: pass://acme-prd-bdo1-talos-apps-01/rclone/drive-password
    - secretKey: drive-token
      remoteRef:
        key: pass://acme-prd-bdo1-talos-apps-01/rclone/drive-token
