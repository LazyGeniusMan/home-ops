apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
patches:
  # Stg-only static `lan` MAC (specs unchanged from base).
  - path: talos-vm-stg-patch.yaml
    target:
      kind: VirtualMachine
      name: talos-vm
