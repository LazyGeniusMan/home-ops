# Dev: minimal specs (1 core / 1Gi / 10Gi — boot+DHCP only) + static `lan` MAC.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
patches:
  - path: talos-vm-dev-patch.yaml
    target:
      kind: VirtualMachine
      name: talos-vm
