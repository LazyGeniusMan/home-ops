apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
patches:
  # Prd-only static `lan` MAC (specs unchanged from base).
  - path: talos-vm-prd-patch.yaml
    target:
      kind: VirtualMachine
      name: talos-vm
