# Narrow cross-namespace handoff grant for the headlamp ESO reader (§13.3).
# ESO mirrors the FirstInstance handoff Secrets (`zitadel-bootstrap-credentials`
# + `zitadel-bootstrap-outputs`, both in the `zitadel` namespace) into this
# namespace's `headlamp-terraform-vars` Secret through the cross-namespace
# `headlamp-zitadel` SecretStore (base/terraform.yaml) — via this ESO mirror (no `data.terraform_remote_state` reads; no headlamp-terraform-remote-state-reader grant is used).
# The store authenticates as THIS namespace SA `eso-zitadel-handoff-reader`,
# so the SA needs get/list/watch on the two handoff Secrets only
# (resourceNames pins both) plus selfsubjectrulesreviews create (ESO
# Kubernetes-provider validation) — and nothing else.
# Ownership note: the Role + RoleBinding below carry explicit
# `metadata.namespace: zitadel` but are OWNED BY the headlamp component — they
# ship in the headlamp base dir so they reconcile (and prune) with the app.
# Same-namespace `varsFrom` CANNOT cross namespaces (no namespace field),
# which is why the ESO mirror + this RBAC is the mechanism.
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: headlamp-zitadel-handoff-reader
  namespace: zitadel
  labels:
    app.kubernetes.io/name: headlamp
    app.kubernetes.io/part-of: headlamp
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["zitadel-bootstrap-credentials", "zitadel-bootstrap-outputs"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["authorization.k8s.io"]
    resources: ["selfsubjectrulesreviews"]
    verbs: ["create"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: headlamp-zitadel-handoff-reader
  namespace: zitadel
  labels:
    app.kubernetes.io/name: headlamp
    app.kubernetes.io/part-of: headlamp
subjects:
  - kind: ServiceAccount
    name: eso-zitadel-handoff-reader
    namespace: headlamp
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: headlamp-zitadel-handoff-reader
