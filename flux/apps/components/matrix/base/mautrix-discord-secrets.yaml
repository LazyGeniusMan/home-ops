# Mautrix Discord secrets (Git holds remoteRefs only; base holds __PROTON_PASS_BASE__ placeholders).
# - mautrix-discord: seven tokens from ONE Proton Pass prefix (bot-token, as-token +
#   hs-token registration pair, avatar-proxy-key, direct-media server-key,
#   provisioning shared-secret, double-puppet-shared-secret). Entrypoint reads them from env.
# - mautrix-discord-db-credentials: connection URL for the colocated Cluster, composed from
#   the single db-password vault field (same field feeds mautrix-discord-db-app-secret below).
# - mautrix-discord-db-app-secret: CNPG initdb secret (username MUST equal
#   spec.bootstrap.initdb.owner), password from the same db-password field.
# Seed with pass-cli before first install; Deployment pends until ESO syncs.
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mautrix-discord
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: proton-pass
    kind: ClusterSecretStore
  target:
    name: mautrix-discord
    template:
      data:
        bot-token: "{{ .botToken }}"
        as-token: "{{ .asToken }}"
        hs-token: "{{ .hsToken }}"
        avatar-proxy-key: "{{ .avatarProxyKey }}"
        direct-media-server-key: "{{ .directMediaServerKey }}"
        provisioning-shared-secret: "{{ .provisioningSharedSecret }}"
        double-puppet-shared-secret: "{{ .doublePuppetSharedSecret }}"
  data:
    - secretKey: botToken
      remoteRef:
        key: __PROTON_PASS_BASE__/mautrix-discord/bot-token
    - secretKey: asToken
      remoteRef:
        key: __PROTON_PASS_BASE__/mautrix-discord/as-token
    - secretKey: hsToken
      remoteRef:
        key: __PROTON_PASS_BASE__/mautrix-discord/hs-token
    - secretKey: avatarProxyKey
      remoteRef:
        key: __PROTON_PASS_BASE__/mautrix-discord/avatar-proxy-key
    - secretKey: directMediaServerKey
      remoteRef:
        key: __PROTON_PASS_BASE__/mautrix-discord/direct-media-server-key
    - secretKey: provisioningSharedSecret
      remoteRef:
        key: __PROTON_PASS_BASE__/mautrix-discord/provisioning-shared-secret
    - secretKey: doublePuppetSharedSecret
      remoteRef:
        key: __PROTON_PASS_BASE__/mautrix-discord/double-puppet-shared-secret
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mautrix-discord-db-credentials
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: proton-pass
    kind: ClusterSecretStore
  target:
    name: mautrix-discord-db-credentials
    template:
      data:
        connection-url: "postgres://discord:{{ .password }}@mautrix-discord-db-rw.matrix.svc:5432/discord?sslmode=require"
  data:
    - secretKey: password
      remoteRef:
        key: __PROTON_PASS_BASE__/mautrix-discord/db-password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mautrix-discord-db-app-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: proton-pass
    kind: ClusterSecretStore
  target:
    name: mautrix-discord-db-app-secret
    template:
      type: kubernetes.io/basic-auth
      data:
        username: discord
        password: "{{ .password }}"
  data:
    - secretKey: password
      remoteRef:
        key: __PROTON_PASS_BASE__/mautrix-discord/db-password
