apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
patches:
  # Stg-only static `lan` MAC (specs unchanged from base).
  - path: win11-vm-stg-patch.yaml
    target:
      kind: VirtualMachine
      name: win11-vm
