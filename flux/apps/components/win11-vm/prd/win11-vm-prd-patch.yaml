# Prd-only patch: static `lan` MAC (specs unchanged from base). The MAC is a literal
# (plain string, no ESO) — mirror it manually at pass://acme-prd-bdo1-talos-apps-01/win11-vm/lan-mac.
- op: add
  path: /spec/template/spec/domain/devices/interfaces/1/macAddress
  # vault: pass://acme-prd-bdo1-talos-apps-01/win11-vm/lan-mac
  value: 52:54:00:03:0B:01
